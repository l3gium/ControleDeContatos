@model UsuarioModel
@{
    ViewData["Title"] = "Confirmação de apagar usuário";
}

<div class="text-center">
    <h3>Realmente deseja apagar o contato (@Model.Nome)</h3>
    <br />
    <br />
    <a class="btn btn-primary" role="button" asp-controller="Usuario" asp-action="Index">Cancelar</a>
    <a class="btn btn-danger" role="button" asp-controller="Usuario" asp-route-id="@Model.Id" asp-action="Apagar">Confirmar</a>

</div>
